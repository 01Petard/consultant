spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/volunteer?serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: kjiolluy711
    hikari:
      idleTimeout: 60000    # 1分钟
      maxLifetime: 1800000  # 30分钟
      minimum-idle: 5
      maximum-pool-size: 20
  data:
    redis:
      host: localhost
      port: 16379
      password: kjiolluy711
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

mybatis:
  configuration:
    map-underscore-to-camel-case: true

mybatis-plus:
  global-config:
    db-config:
      id-type: auto                      # 主键类型（自动递增）
      logic-delete-value: 1              # 逻辑删除标记值（1 表示已删除）
      logic-not-delete-value: 0          # 逻辑未删除标记值（0 表示未删除）
    banner: false
  mapper-locations: classpath:mapper/**/*.xml

logging:
  level:
    root: INFO
    dev.langchain4j: INFO
  file:
    name: logs/app.log
  config: classpath:logback-spring.xml


langchain4j:
  open-ai:
    chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: sk-e47bfe79746d4fe48adeb2afeff2240c
      model-name: qwen-plus
      log-requests: true
      log-responses: true
    streaming-chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: sk-e47bfe79746d4fe48adeb2afeff2240c
      model-name: qwen-plus
      log-requests: true
      log-responses: true
    embedding-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: sk-e47bfe79746d4fe48adeb2afeff2240c
      model-name: text-embedding-v3
      log-requests: true
      log-responses: true
      max-segments-per-batch: 10
  community:
    redis:
      host: localhost
      port: 16379



